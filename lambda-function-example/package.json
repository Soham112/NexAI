{
  "name": "nexai-lambda-bedrock-agent",
  "version": "1.0.0",
  "description": "Lambda function to connect NextAI frontend with Bedrock Agent",
  "main": "index.js",
  "scripts": {
    "deploy": "zip -r function.zip . && aws lambda update-function-code --function-name nexai-bedrock-agent --zip-file fileb://function.zip",
    "test": "node test.js"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-agent-runtime": "^3.450.0"
  },
  "devDependencies": {
    "aws-sdk": "^2.1500.0"
  },
  "keywords": [
    "aws",
    "lambda",
    "bedrock",
    "agent",
    "nexai"
  ],
  "author": "NexAI Team",
  "license": "MIT"
}
